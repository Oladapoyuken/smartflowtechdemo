spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/smartflowtechdemo_db
    username: postgres
    password: password
    pool-size: 30

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

